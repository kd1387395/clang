#include<stdio.h>
main()
{
	int no = 0;
	int con = 0;
	int sum = 0;
	do {
		sum += no;
		con++;
		printf("®”(-999‚ÅI—¹)?");
		scanf("%d", &no);
	} while (no != -999);
	con--;
	printf("‡Œv=%d\n", sum);
	printf("•½‹Ï=%f\n", (float)sum / (float)con);

}